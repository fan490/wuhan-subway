# 武汉地铁WebGIS系统 — 课堂汇报纲要

## 一、项目概述（1页）

- 项目名称：武汉地铁查询与最少换乘路径规划系统
- 课程：WebGIS
- 技术栈总览：Vue 3 + Vite + Leaflet + Node.js/Express + PostgreSQL/PostGIS + Neo4j + Docker
- 核心功能一句话：在高德地图上可视化武汉地铁线网，支持站点查询、线路筛选和最少换乘路径规划
- 数据规模：4条地铁线路、34个站点（含换乘站）、覆盖武汉市5个行政区

## 二、第三章：基于STAC规范的数据目录设计（1页）

- **Catalog（根目录）**：`wuhan-metro-course-catalog`，通过 `links` 关联子目录 Collection
- **Collection（集合）**：`wuhan-metro-network`，定义空间范围 Bbox `[114.209, 30.468, 114.424, 30.784]`（武汉市中心），时间范围 `2026-05-25` 至今，EPSG:4326
- **Item（数据项）**：以 GeoJSON Feature 描述数据资产，`assets` 字段关联 JSON / XML 两种数据源
- **空间索引**：Bbox 空间范围 + temporal interval 时间范围，支持按时空维度检索
- **Web调用**：前端 Leaflet + 高德底图可视化；后端 `/api/stac/catalog`、`/api/stac/collection`、`/api/stac/items/*` 三个端点提供STAC元数据
- **多源消歧**：本项目为单一地铁数据源，STAC结构预留了多 Item 扩展能力，`properties` 字段可用于未来多源语义对齐

## 三、第四章：XML与JSON数据格式（1页）

- **双格式存储**：同一套武汉地铁数据（线路、站点、换乘关系）分别以 JSON 和 XML 两种格式组织
- **JSON**（`wuhan-metro.json`）：三层结构 `meta → lines → stations`，适合 RESTful API 传输与前端直接解析
- **XML**（`wuhan-metro.xml`）：层级结构 `MetroDataset → Lines → Line → StationRef`，自描述性强，适合文档交换与 Schema 验证
- **API访问**：`/api/source-data/json` 返回 JSON，`/api/source-data/xml` 返回 XML（`Content-Type: application/xml`）
- **对比总结**：JSON 紧凑高效，XML 严谨自描述；两种格式表达同一数据，体现不同场景下的格式选择

## 四、第五章：SQL与NoSQL双数据库架构（2页）

### 4.1 数据库选型与部署
- **PostgreSQL 15 + PostGIS 3.3**：空间数据存储，支持 Point / LineString 几何类型与 GIST 空间索引
- **Neo4j 5 Community**：图数据存储，Station / MetroLine 节点，NEXT_TO / ON_LINE 关系
- Docker Compose 一键部署，初始化脚本自动建表建约束并导入数据

### 4.2 数据存储对照
| | PostgreSQL/PostGIS | Neo4j |
|---|---|---|
| 站点 | `metro_stations` 表，GEOMETRY(Point, 4326) | `Station` 节点，含经纬度属性 |
| 线路 | `metro_lines` 表 | `MetroLine` 节点 |
| 连通关系 | `metro_segments` 表，GEOMETRY(LineString, 4326) | `NEXT_TO` 边，含线路信息 |
| 归属关系 | 通过 JOIN 查询 | `ON_LINE` 边，含序号 |
| 索引 | GIST 空间索引 | 唯一性约束 + 自动标记换乘站 |
| 优势 | 空间范围查询、缓冲区分析 | 图遍历、多跳路径查询 |

### 4.3 Mashup地图（选做）
- 高德地图瓦片作为底图，站点用自定义 DivIcon（换乘站实心、普通站空心），线路按颜色区分（2号线红 / 4号线绿 / 7号线黄 / 8号线青）

## 五、前端功能亮点（2页）

### 5.1 地图交互
- 单击站点：弹出信息窗口，自动填入路径规划起终点
- 单击线路：高亮该线路，淡化其余线路
- 站点标记状态同步：路线起终点有特殊标记（绿色"起"、红色"终"）
- 地图飞行动画：选中站点时平滑过渡

### 5.2 站点搜索
- 200ms防抖自动补全
- 支持按站名、行政区、类别、线路名多维搜索
- 搜索结果可直接设为起点/终点

### 5.3 最少换乘路径规划
- 自实现 Dijkstra 变体算法，代价函数：换乘次数（主）→ 站数（次）
- 路径结果展示：总站数、换乘次数、分段指引
- 地图上三层可视化：白色外发光 + 深色边框 + 彩色虚线芯
- 虚线行走动画（CSS `dash-march` 关键帧）
- 自动缩放至路径范围

### 5.4 数据库健康监控
- 顶部导航栏实时显示PostgreSQL和Neo4j连接状态（绿/红指示灯）
- 30秒定时轮询 `/api/health`

## 六、技术架构总览（1页）

```
浏览器 (Vue 3 + Leaflet)
    │
    ▼
Vite Dev Server (:5173) ──proxy──▶ Express API (:3000)
                                      │
                          ┌───────────┼───────────┐
                          ▼           ▼           ▼
                    PostgreSQL    Neo4j      静态数据
                    /PostGIS      (图数据库)  (JSON/XML/STAC)
                    (空间数据库)
```

## 七、结束语（1页）

- 期待老师的批评指导
